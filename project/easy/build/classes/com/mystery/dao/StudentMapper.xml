<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mystery.dao.StudentMapper">
	<resultMap id="BaseResultMap"
		type="com.mystery.entity.Student">
		<!-- WARNING - @mbg.generated -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="name" jdbcType="VARCHAR" property="name" />
		<result column="sex" jdbcType="VARCHAR" property="sex" />
		<result column="birth" jdbcType="DATE" property="birth" />
		<result column="department" jdbcType="VARCHAR"
			property="department" />
		<result column="address" jdbcType="VARCHAR" property="address" />
		<result column="LV" jdbcType="VARCHAR" property="lv" />
	</resultMap>
	<!-- <select id="getStudentNumber" parameterType="java.lang.Integer"> select 
		count(1) from student where 1=1 <if test="department!=null and department!=''">and 
		department=#{department}</if> <if test="sex!=null and sex!=''">and sex=#{sex}</if> 
		</select> -->

	<select id="getStudentNumber" parameterType="BaseResultMap">
		select * from student where 1=1
		<if test="department!=null and department!=''">and department=#{department}</if>
		<if test="sex!=null and sex!=''">and sex=#{sex}</if>
	</select>

</mapper>